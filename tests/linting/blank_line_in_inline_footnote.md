# Blank line in an inline footnote

This note is accidentally split.^[First paragraph.

Second paragraph.]

This inline note may wrap across lines.^[First line.
Second line.]

This marker is escaped: \^[literal text

and a closing bracket].

This uses ^[ as literal notation.

There is no apparent closing bracket later.
