# The drifted `5.`/`6.` markers in this fixture expose a pre-existing
# LIST-inside-LIST mis-nesting (see TODO.md). Under pandoc 3.10 that shape
# no longer round-trips, so this regression is pinned to the target it was
# written against.
[compat]
pandoc = "3.9"
