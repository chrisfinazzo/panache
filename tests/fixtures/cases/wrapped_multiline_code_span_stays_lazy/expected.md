- item text here that is quite long and will be wrapped by the formatter, e.g.:
  `r    x <- 1  `
- next

> quoted text here that is quite long and will be wrapped by the formatter,
> e.g.: `r  x <- 1  `
