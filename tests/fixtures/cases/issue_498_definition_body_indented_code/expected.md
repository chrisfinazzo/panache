`opts`

:   Splitting options.

    ```
    `path_template`
    :   template used to generate filepaths

        Default is `"chunk-%n"` (string)

    `number_sections`
    :   whether sections should be numbered
    ```
